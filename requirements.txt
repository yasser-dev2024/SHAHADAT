# مكتبات Django الأساسية
Django==5.2.4

# لتشغيل المشروع على الخوادم (Railway, Render, إلخ)
gunicorn

# لإدارة الملفات الثابتة (CSS/JS/صور)
whitenoise

# لمعالجة الصور (مطلوبة لرفع الشهادات والشعارات)
pillow

# لدعم قواعد بيانات Railway (PostgreSQL وغيرها)
dj-database-url

# مكتبة اختيارية لحماية المفاتيح (يمكنك إضافتها لاحقاً)
python-dotenv
